// Copy to config.js and fill in your Google Apps Script webhook URL.
// config.js is gitignored — never commit real credentials.
window.SCDNR_CONFIG = {
  WEBHOOK_URL: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec'
};
